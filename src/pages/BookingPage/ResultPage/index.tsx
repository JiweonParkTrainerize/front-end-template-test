import React from 'react';

const ResultPage: React.FC = () => {
    // TODO handle navigation params if to render success, user exist or not found page
    return (
        <>
            <div>Appointment was booked</div>
        </>
    );
};

export default ResultPage;
